# ── Core API framework ──────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
httpx>=0.27.0
pydantic>=2.7.0

# ── OpenAI Privacy Filter (local, via HuggingFace) ──────────────────────────
transformers>=4.50.0
torch>=2.2.0          # CPU-only install: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Optional: accelerate>=0.27.0  # Faster inference on GPU

# ── Microsoft Presidio (local) ───────────────────────────────────────────────
presidio-analyzer>=2.2.355
presidio-anonymizer>=2.2.355
spacy>=3.7.0

# ── NOTE: After installing, run the spaCy model download: ───────────────────
#    python -m spacy download en_core_web_lg
#    (or en_core_web_sm for lighter footprint)

# ── Azure AI Language (cloud API — no extra SDK needed, uses httpx) ──────────
# The Azure AI Language PII endpoint is called via httpx directly (REST).
# If you prefer the SDK: azure-ai-textanalytics>=5.3.0

# ── Optional: Azure SDK (uncomment if preferred over raw REST) ───────────────
# azure-ai-textanalytics>=5.3.0
# azure-core>=1.29.0
